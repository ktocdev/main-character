# Core
chromadb>=0.5          # local vector store (bundles all-MiniLM-L6-v2 embeddings)
anthropic>=0.40        # Claude API — companion + future metadata extraction
python-dotenv>=1.0     # .env config
